language: python

python:
  - 2.7
  - 3.3
#  - pypy we require at least pypy >= 2.0 travis doesn't it, yet.

install:
  - make dev

script:
  - find relief -iname "*.py" | xargs pyflakes
  - py.test -vv relief
  - sphinx-build -aEWb doctest docs docs/_build
#  - sphinx-build -aEWb linkcheck docs docs/_build
